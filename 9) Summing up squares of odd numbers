#include <stdio.h>

int main() {
    int n, sum;

    printf("Enter the value of n: ");
    scanf("%d", &n);

    sum = n*(n*(n+1)/2)*(n*(n+1)/2);

    printf("Sum of the square of first %d odd numbers: %d\n", n, sum);

    return 0;
}
